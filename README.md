# Contact Info Parser

Тестовое задание: парсер сайта для извлечения контактной информации (emails и телефоны).

---

## 📂 Структура проекта

    contact-info-parser/
    ├── parser/
    │ ├── parser.py        # Инициализация класса
    │ ├── crawler.py       # BFS-обход
    │ ├── extractor.py     # Нормализация email и телефонов
    │ ├── url_handler.py   # Проверка url
    │ ├── utils.py
    │   ├── export.py      # Сохранение результатов в CSV и TXT
    │   ├── logger.py      # Настройка логгера
    │   └── config.py      # Параметры
    ├── main.py
    ├── requirements.txt   # Для pip установки зависимостей
    ├── poetry.lock        # Для poetry установки зависимостей
    ├── pyproject.toml
    └── README.md          # Документация

---

## ⚙ Установка и запуск

1.1 Через Poetry:
```bash
poetry install
```

1.2 Через pip:
```bash
python -m venv venv
# Linux/macOS
source venv/bin/activate
# Windows
venv\Scripts\activate

pip install -r requirements.txt
```

2 Настроить конфигурацию в parser/utils/config.py по необходимости


3.1 Запуск через poetry:
```bash
poetry run python main.py {url}
```

3.2 Запуск через pip:
```bash
python main.py {url}
```

4. Для запуска с экспортом:
```bash
python main.py {url} --csv output.csv --txt output.txt
```

---

## 🔧 Настройки

Файл parser/utils/config.py с установленными по-умолчанию значениями:

    request_timeout = 5      # Таймаут для HTTP запросов, сек
    max_pages = 50           # Максимальное число страниц для обхода
    max_depth = 3            # Максимальная глубина обхода
    phone_region = "RU"      # Регион для валидации номеров телефонов
    user_agent
    allowed_schemes = ("http", "https")

---

## 📌 Важное
    
    Обходит только страницы одного домена через BFS
    Проверка robots.txt перед обходом
    Проверка телефонов с помощью phonenumbers
    Проверка email через email_validator

